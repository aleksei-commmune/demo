name: "Formatting"
description: "Lint + Prettier + CSpell"
inputs:
  dir:
    description: formatting folder
    required: true
runs:
  using: "composite"
  steps:
    - name: Lint
      working-directory: ${{ inputs.dir }}
      run: npm run lint
      shell: bash
